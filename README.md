# ТЕХНИЧЕСКОЕ ЗАДАНИЕ
## Веб-приложение "Nexus" - Система совместного рисования

### 1. ОБЩЕЕ ОПИСАНИЕ ПРОЕКТА

#### 1.1 Полное наименование проекта
Nexus - Веб-приложение для совместного рисования и организации рабочих пространств

#### 1.2 Краткая характеристика проекта
Веб-приложение представляет собой полнофункциональную систему для создания и совместного редактирования интерактивных досок (whiteboard) с организацией в рабочие пространства. Проект включает frontend на базе HTML/CSS/JavaScript и backend на Node.js с PostgreSQL.

#### 1.3 Цель и назначение
Проект предназначен для:
- Организации командной работы над визуальными проектами
- Создания интерактивных диаграмм, схем и презентаций
- Совместного рисования и мозгового штурма в реальном времени
- Управления доступами и ролями в рабочих пространствах

#### 1.4 Основные функции
- Создание и управление рабочими пространствами
- Совместное редактирование интерактивных досок
- Богатый набор элементов рисования (фигуры, текст, изображения)
- Система слоев и группировки элементов
- История действий с возможностью отмены/повтора
- Ролевая модель доступа

---

### 2. ТРЕБОВАНИЯ К ПОЛЬЗОВАТЕЛЯМ И СИСТЕМЕ

#### 2.1 Категории пользователей

| Категория | Описание | Права доступа |
|-----------|----------|---------------|
| **Владелец рабочего пространства** | Создатель рабочего пространства | Полный контроль над пространством, управление участниками, создание/удаление досок |
| **Администратор** | Приглашенный администратор | Управление участниками, создание/редактирование досок |
| **Участник** | Обычный член команды | Создание и редактирование досок, добавление контента |
| **Наблюдатель** | Пользователь с правами только на просмотр | Просмотр содержимого без возможности редактирования |

#### 2.2 Требования к программному обеспечению

**Frontend:**
- Современные веб-браузеры с поддержкой HTML5 Canvas
- JavaScript ES6+
- CSS3 с поддержкой Flexbox и Grid

**Backend:**
- Node.js версии 16+
- PostgreSQL версии 12+
- Docker для контейнеризации

#### 2.3 Требования к аппаратному обеспечению

**Минимальные требования:**
- Процессор: 2-core CPU
- Оперативная память: 4 GB RAM
- Хранилище: 10 GB свободного места
- Сетевое подключение: 10 Mbps

**Рекомендуемые требования:**
- Процессор: 4-core CPU
- Оперативная память: 8 GB RAM
- Хранилище: 50 GB SSD
- Сетевое подключение: 50 Mbps

---

### 3. ФУНКЦИОНАЛЬНЫЕ ТРЕБОВАНИЯ

#### 3.1 Система аутентификации

**Основные сценарии:**
1. **Регистрация пользователя**
   - Предусловие: Пользователь не авторизован
   - Действия: Ввод email, имени, пароля → Отправка на сервер → Создание учетной записи
   - Постусловие: Пользователь зарегистрирован и авторизован

2. **Вход в систему**
   - Предусловие: Пользователь имеет учетную запись
   - Действия: Ввод email и пароля → Проверка учетных данных → Выдача JWT токена
   - Постусловие: Пользователь авторизован

#### 3.2 Управление рабочими пространствами

**Основные функции:**
- Создание рабочего пространства с указанием названия и описания
- Приглашение участников по email с назначением ролей
- Управление участниками (изменение ролей, исключение)
- Передача прав владельца
- Настройка аватара и описания рабочего пространства

**Сценарий создания рабочего пространства:**
1. Пользователь нажимает "Создать рабочее пространство"
2. Вводит название и описание
3. Выбирает аватар (опционально)
4. Система создает пространство и назначает пользователя владельцем
5. Отображается страница управления пространством

#### 3.3 Управление досками

**Основные функции:**
- Создание досок внутри рабочих пространств *(в разработке - используется mock-данные)*
- Настройка фона доски (цвет, сетка) *(частично реализовано)*
- Блокировка доски от редактирования *(в разработке)*
- Экспорт/импорт досок в формате JSON *(не реализовано)*

**Типы элементов доски:**
- **Фигуры**: Прямоугольники, круги, треугольники, звезды
- **Линии и стрелки**: Прямые линии, стрелки, умные соединители
- **Текст**: Форматированный текст с поддержкой шрифтов и цветов
- **Изображения**: Загрузка и вставка изображений
- **Файлы**: Прикрепление документов (PDF, DOC, XLS)
- **Свободное рисование**: Рисование от руки с настройкой толщины и цвета

#### 3.4 Система слоев и группировки

**Функциональность:**
- Управление Z-индексом элементов (вверх/вниз, на передний/задний план)
- Группировка нескольких элементов в одну группу
- Разгруппировка элементов
- Визуальные индикаторы группировки

#### 3.5 История и управление версиями

**Функциональность:**
- Автоматическое отслеживание всех изменений
- Ручное создание снимков состояния доски
- Отмена/повтор действий (Undo/Redo)
- Навигация по истории изменений
- Автоматические снимки каждые 10 минут

#### 3.6 Свойства элементов

**Настраиваемые параметры:**
- Позиция и размер (X, Y, ширина, высота)
- Цвета (заливка, обводка, текст)
- Типографика (шрифт, размер, начертание)
- Поворот и opacity
- Специфические свойства элементов

---

### 4. ТРЕБОВАНИЯ К ИНТЕРФЕЙСУ

#### 4.1 Общие принципы дизайна
- Чистый, минималистичный интерфейс
- Интуитивная навигация
- Адаптивный дизайн для различных устройств
- Темная/светлая тема (планируется)

#### 4.2 Основные страницы

**Главная страница (index.html):**
- Список доступных рабочих пространств
- Панель навигации с профилем пользователя
- Кнопка создания нового рабочего пространства

**Страница рабочего пространства (dashboard.html):**
- Список досок рабочего пространства
- Панель инструментов для создания досок
- Фильтры и поиск по доскам

**Редактор доски (dashboard.html?board=ID):**
- Центральная область рисования (Canvas)
- Панель инструментов слева
- Панель свойств справа
- Панель слоев снизу
- Панель истории в правом верхнем углу

**Управление рабочим пространством (workspace-settings.html):**
- Настройки пространства (название, описание, аватар)
- Список участников с ролями
- Форма приглашения новых участников

#### 4.3 Панели интерфейса

**Панель инструментов (Toolbar):**
- Выбор типа элемента (фигура, текст, изображение и т.д.)
- Настройки цвета и толщины линии
- Инструменты выделения и трансформации

**Панель свойств (Properties):**
- Динамическое отображение свойств выбранного элемента
- Поля ввода для числовых значений
- Палитра цветов
- Выпадающие списки для выбора опций

**Панель слоев (Layers):**
- Дерево элементов с возможностью перетаскивания
- Индикаторы видимости и блокировки
- Групповые операции

**Панель истории (History):**
- Список последних действий
- Кнопки Undo/Redo
- Миниатюры снимков состояния

#### 4.4 Навигация и взаимодействие
- Клавиатурные shortcuts (Ctrl+Z, Delete, Escape)
- Drag & drop для загрузки файлов
- Контекстные меню правой кнопкой мыши
- Подсказки (tooltips) для инструментов

---

### 5. ТРЕБОВАНИЯ К БЕЗОПАСНОСТИ

#### 5.1 Аутентификация и авторизация
- JWT токены для сессий пользователей
- bcrypt хэширование паролей (12 раундов)
- Защита от brute-force атак
- Автоматическое истечение сессий

#### 5.2 Управление доступом
- Ролевая модель доступа (owner, admin, member, viewer)
- Проверка прав на каждом API endpoint
- Изоляция данных между рабочими пространствами
- Невозможность удаления владельца пространства

#### 5.3 Защита данных
- Валидация всех входных данных
- Защита от SQL-инъекций через параметризованные запросы
- Ограничение размера загружаемых файлов (10MB)
- Безопасное хранение файлов в изолированной директории

#### 5.4 Сетевая безопасность
- HTTPS протокол для всех соединений
- CORS политика для ограничения доменов
- Rate limiting для API endpoints
- Логирование подозрительной активности

---

### 6. API СПЕЦИФИКАЦИЯ

#### 6.1 Реализованные API Endpoints

**Аутентификация:**
- `POST /api/auth/login` - Вход в систему ✅
- `POST /api/auth/register` - Регистрация пользователя ✅

**Рабочие пространства:**
- `GET /api/workspaces` - Список рабочих пространств пользователя ✅
- `POST /api/workspaces` - Создание рабочего пространства ✅
- `GET /api/workspaces/:id` - Получение деталей рабочего пространства ✅
- `PUT /api/workspaces/:id` - Обновление рабочего пространства ✅
- `DELETE /api/workspaces/:id` - Удаление рабочего пространства ✅

**Управление участниками:**
- `POST /api/workspaces/:id/invite` - Приглашение участника ✅
- `PUT /api/workspaces/:id/members/:userId` - Изменение роли участника ✅
- `DELETE /api/workspaces/:id/members/:userId` - Удаление участника ✅
- `POST /api/workspaces/:id/transfer-ownership` - Передача прав владельца ✅

**Элементы досок:**
- `GET /api/boards/:boardId/elements` - Получение элементов доски ✅
- `POST /api/boards/:boardId/elements` - Создание элемента ✅
- `PUT /api/boards/:boardId/elements/:elementId` - Обновление элемента ✅
- `DELETE /api/boards/:boardId/elements/:elementId` - Удаление элемента ✅
- `POST /api/boards/:boardId/elements/group` - Группировка элементов ✅
- `POST /api/boards/:boardId/elements/ungroup` - Разгруппировка элементов ✅

**История и snapshots:**
- `POST /api/boards/:boardId/snapshot` - Создание снимка состояния ✅
- `GET /api/boards/:boardId/history` - Получение истории ✅
- `POST /api/boards/:boardId/undo` - Отмена действия ✅

**Файлы:**
- `POST /api/boards/:boardId/upload` - Загрузка файла ✅
- `GET /api/files/:id` - Получение файла ✅

**Приглашения:**
- `GET /api/invitations/:token` - Получение деталей приглашения ✅
- `POST /api/workspaces/:id/invitations/:invitationId/accept` - Принятие приглашения ✅
- `POST /api/workspaces/:id/invitations/:invitationId/reject` - Отклонение приглашения ✅

#### 6.2 Отсутствующие API Endpoints

**Управление досками (требуются для полной интеграции):**
- `GET /api/workspaces/:workspaceId/boards` - Получение досок рабочего пространства ❌
- `POST /api/workspaces/:workspaceId/boards` - Создание доски в рабочем пространстве ❌
- `GET /api/boards/:id` - Получение деталей доски ❌
- `PUT /api/boards/:id` - Обновление доски ❌
- `DELETE /api/boards/:id` - Удаление доски ❌

**Дополнительная функциональность:**
- `POST /api/boards/:id/export` - Экспорт доски ❌
- `POST /api/boards/:id/import` - Импорт доски ❌

---

### 8. ТРЕБОВАНИЯ К ХОСТИНГУ И РАЗВЕРТЫВАНИЮ

#### 6.1 Архитектура развертывания

##### Вариант 1: Docker (рекомендуемый для всех платформ)
```yaml
services:
  nexus-backend:
    build: .
    ports:
      - "3001:3001"
      - "3002:3002"  # WebSocket порт
    volumes:
      - .:/app
      - /app/node_modules
      - uploads:/app/uploads  # Том для загружаемых файлов
    environment:
      - NODE_ENV=development
      - PORT=3001
      - DATABASE_URL=postgresql://postgres:password@db:5432/nexus_db
      - JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-123456789
      - FRONTEND_URL=http://localhost:3000
    depends_on:
      - db
    command: npm run dev

  db:
    image: postgres:15-alpine
    environment:
      - POSTGRES_DB=nexus_db
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=password
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./database_schema.sql:/docker-entrypoint-initdb.d/01-schema.sql

volumes:
  postgres_data:
  uploads:
```

##### Вариант 2: Развертывание на Windows 10

**2.1 Предварительные требования:**
- Windows 10 версии 1903 или выше
- Windows PowerShell (предустановлен)
- Доступ администратора

**2.2 Установка Node.js:**
```powershell
# Скачать и установить Node.js LTS
# Перейти на https://nodejs.org/ и скачать установщик
# Или использовать winget:
winget install OpenJS.NodeJS.LTS
```

**2.3 Установка PostgreSQL:**
```powershell
# Скачать PostgreSQL для Windows
# Перейти на https://www.postgresql.org/download/windows/
# Запустить установщик от имени администратора

# Или использовать Chocolatey (если установлен):
choco install postgresql
```

**2.4 Настройка базы данных:**
```powershell
# Открыть PowerShell от имени администратора
# Перейти в директорию PostgreSQL bin
cd "C:\Program Files\PostgreSQL\15\bin"

# Подключиться к PostgreSQL
.\psql.exe -U postgres

# В psql выполнить:
CREATE DATABASE nexus_db;
GRANT ALL PRIVILEGES ON DATABASE nexus_db TO postgres;
\q

# Запустить SQL схему
.\psql.exe -U postgres -d nexus_db -f "C:\path\to\your\project\database_schema.sql"
```

**2.5 Настройка переменных окружения:**
```powershell
# Создать файл .env в корне проекта
# Отредактировать с реальными значениями:
DATABASE_URL=postgresql://postgres:your_password@localhost:5432/nexus_db
JWT_SECRET=your-super-secret-jwt-key-here-change-this-in-production
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
FRONTEND_URL=http://localhost:3000
PORT=3001
NODE_ENV=development
```

**2.6 Установка зависимостей и запуск:**
```powershell
# Перейти в директорию проекта
cd "C:\path\to\your\project"

# Установить зависимости
npm install

# Запустить в режиме разработки
npm run dev

# Или для продакшена
npm start
```

**2.7 Проверка установки:**
```powershell
# В отдельном окне PowerShell проверить API
Invoke-WebRequest -Uri "http://localhost:3001/api/workspaces" -Method GET

# Проверить подключение к БД
cd "C:\Program Files\PostgreSQL\15\bin"
.\psql.exe -U postgres -d nexus_db -c "\dt"
```

##### Вариант 3: Развертывание на Linux (Ubuntu/Debian)

**3.1 Установка Node.js:**
```bash
# Используя NodeSource репозиторий
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt-get install -y nodejs

# Или используя snap
sudo snap install node --classic
```

**3.2 Установка PostgreSQL:**
```bash
# Обновить пакеты
sudo apt update

# Установить PostgreSQL
sudo apt install postgresql postgresql-contrib

# Запустить и включить службу
sudo systemctl start postgresql
sudo systemctl enable postgresql
```

**3.3 Настройка базы данных:**
```bash
# Переключиться на пользователя postgres
sudo -u postgres psql

# В psql выполнить:
CREATE DATABASE nexus_db;
CREATE USER nexus_user WITH PASSWORD 'secure_password';
GRANT ALL PRIVILEGES ON DATABASE nexus_db TO nexus_user;
\q

# Запустить SQL схему
sudo -u postgres psql -d nexus_db -f /path/to/database_schema.sql
```

**3.4 Настройка Nginx (опционально):**
```bash
# Установить Nginx
sudo apt install nginx

# Создать конфигурацию сайта
sudo nano /etc/nginx/sites-available/nexus
```

```nginx
server {
    listen 80;
    server_name your-domain.com;

    location / {
        proxy_pass http://localhost:3001;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
}
```

```bash
# Включить сайт
sudo ln -s /etc/nginx/sites-available/nexus /etc/nginx/sites-enabled/

# Перезапустить Nginx
sudo systemctl restart nginx
```

##### Вариант 4: Использование облачных сервисов

**4.1 Heroku:**
```bash
# Установить Heroku CLI
npm install -g heroku

# Создать приложение
heroku create your-nexus-app

# Добавить PostgreSQL аддон
heroku addons:create heroku-postgresql:hobby-dev

# Развернуть
git push heroku main
```

**4.2 Railway:**
- Подключить GitHub репозиторий
- Автоматически определить Node.js приложение
- Добавить PostgreSQL плагин
- Развернуть одним кликом

**4.3 DigitalOcean App Platform:**
- Подключить репозиторий
- Выбрать Node.js runtime
- Добавить PostgreSQL базу данных
- Настроить переменные окружения
- Развернуть

#### 6.2 Системные требования к хостингу

**Минимальная конфигурация для разработки:**
- **Windows 10 1903+**: 2 CPU cores, 4 GB RAM, 20 GB SSD
- **Ubuntu 18.04+/Debian 10+**: 2 CPU cores, 4 GB RAM, 20 GB SSD
- **macOS 10.15+**: 2 CPU cores, 4 GB RAM, 20 GB SSD
- **Docker**: Windows/Mac/Linux с Docker Desktop

**Производственная конфигурация:**
- 4+ CPU cores
- 8 GB RAM
- 100 GB SSD хранилище
- Load balancer для масштабирования
- SSL сертификаты для HTTPS

**Поддерживаемые платформы:**
- **Операционные системы**: Windows Server 2019+, Ubuntu 18.04+, CentOS 7+, macOS 10.15+
- **Базы данных**: PostgreSQL 12+, MySQL 8.0+ (с модификациями), SQLite (для разработки)
- **Веб-серверы**: Nginx, Apache, IIS
- **Контейнеризация**: Docker, Podman, Kubernetes

#### 6.3 Переменные окружения

Обязательные переменные:
```env
DATABASE_URL=postgresql://user:password@host:port/database
JWT_SECRET=<secure-random-string>
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=email@gmail.com
SMTP_PASS=app-password
FRONTEND_URL=https://yourdomain.com
PORT=3001
NODE_ENV=production
```

#### 6.4 Мониторинг и логирование
- Логи приложения в структурированном формате
- Мониторинг производительности (CPU, память, диск)
- Отслеживание ошибок и исключений
- Резервное копирование базы данных

#### 6.5 Масштабирование
- Горизонтальное масштабирование backend
- Репликация базы данных PostgreSQL
- CDN для статических ресурсов
- Кэширование на уровне приложения

---

### 10. НЕФУНКЦИОНАЛЬНЫЕ ТРЕБОВАНИЯ

#### 7.1 Производительность
- **Время отклика API**: ≤ 500 мс для основных операций
- **Количество одновременных пользователей**: до 1000 активных сессий
- **Время загрузки страницы**: ≤ 3 секунд при нормальном интернет-соединении
- **Плавность анимаций**: 60 FPS для операций рисования

#### 7.2 Масштабируемость
- **Горизонтальное масштабирование**: поддержка кластеризации серверов
- **База данных**: поддержка репликации и шардинга
- **Файловое хранилище**: поддержка CDN для статических ресурсов

#### 7.3 Совместимость
- **Браузеры**: Chrome 90+, Firefox 88+, Safari 14+, Edge 90+
- **Мобильные устройства**: адаптивный дизайн для планшетов и смартфонов
- **Операционные системы**: Windows 10+, macOS 10.15+, Linux (Ubuntu 18.04+)

---

### 9. ТЕКУЩЕЕ СОСТОЯНИЕ РЕАЛИЗАЦИИ

#### 8.1 Реализованная функциональность

**✅ Полностью реализована:**
- Система аутентификации пользователей (регистрация, вход, JWT токены)
- Управление рабочими пространствами (создание, настройка, управление участниками)
- Ролевая модель доступа (owner, admin, member, viewer)
- Система приглашений участников по email
- Расширенная whiteboard функциональность для отдельных досок
- Система элементов (фигуры, текст, изображения, стикеры, рисование от руки)
- Управление слоями и группировка элементов
- История действий (undo/redo)
- Свойства элементов и панели управления
- Система файловых вложений

**🔄 Частично реализована:**
- Интеграция досок с рабочими пространствами (API endpoints отсутствуют, используется mock-данные)
- Настройки досок (фон, сетка, блокировка)

**❌ Не реализована:**
- Проекты внутри рабочих пространств
- Real-time совместная работа (WebSocket/WebRTC)
- Экспорт/импорт досок
- Расширенные настройки интерфейса (темы)

#### 8.2 Архитектурные особенности

**Backend (Node.js/Express):**
- Полная реализация REST API для рабочих пространств
- Частичная реализация API для элементов досок
- Отсутствие CRUD API для самих досок
- Полная система аутентификации и авторизации
- Файловый сервер для вложений

**Frontend (Vanilla JS):**
- Полная реализация интерфейса рабочих пространств
- Расширенная whiteboard функциональность
- Mock-данные для досок (не интегрированы с backend)
- Адаптивный дизайн для различных устройств

**База данных (PostgreSQL):**
- Полная схема для всех сущностей
- Связи между рабочими пространствами, досками и элементами
- Индексы для производительности

#### 8.3 Планы развития

**Краткосрочные задачи (1-2 месяца):**
- Реализация CRUD API для досок
- Интеграция досок с рабочими пространствами
- Реализация настроек досок
- Экспорт/импорт функциональность

**Среднесрочные задачи (3-6 месяцев):**
- Real-time совместная работа (WebSocket)
- Система проектов внутри рабочих пространств
- Расширенные настройки интерфейса
- Мобильная адаптация

**Долгосрочные задачи (6+ месяцев):**
- WebRTC для голосовой/видео коммуникации
- Интеграция с внешними сервисами
- Расширенная аналитика использования
- Плагины и расширения

---

## ЗАКЛЮЧЕНИЕ

Данный документ определяет технические требования к разработке веб-приложения Nexus - системы совместного рисования с организацией рабочих пространств. Проект включает frontend на базе HTML/CSS/JavaScript и backend на Node.js с PostgreSQL.

Проект находится в активной разработке. Система рабочих пространств полностью реализована, whiteboard функциональность реализована для отдельных досок, но интеграция между этими компонентами находится в процессе реализации.

Все требования сформулированы в соответствии с принципами ГОСТ 34.602-89, обеспечивая полноту описания функциональности и технических характеристик системы.

**Дата составления**: 14 января 2026 г.
**Версия документа**: 1.1
